def frequency_counter(s):
    s = list(s)
    while s:
        length = len(s)
        for i in s:
            for x in range(1, len(s)):
                if i = s[x]:
                    print(x)
                else:
                    continue
